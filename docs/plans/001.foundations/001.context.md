# Step 001 — context

No narrow context beyond what is captured in [`context.md`](context.md). All decisions for this step (Pydantic v2 dependency, pyright extra, voyage extra, `code_index = "code_index.cli:app"` entry point) are stated there or in `pyproject.toml`'s existing state from commit `2a84281`.

The verifier reads Build & Test Commands from the repo root `CLAUDE.md`. Anything this step changes there is out of scope — that file is already accurate for Phase 1 (it already notes that `pyright` lands in this phase).

Touch sites for later steps:

- `src/code_index/__main__.py` may import from `src.code_index.cli` once step 005 lands. The coder may either write the import now (it will be unresolved until 005) or stub it until 005.
